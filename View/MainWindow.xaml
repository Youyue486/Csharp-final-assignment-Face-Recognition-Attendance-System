<Window x:Class="Csharp_final_assignment_Face_Recognition_Attendance_System.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Csharp_final_assignment_Face_Recognition_Attendance_System.View"
        xmlns:vm="clr-namespace:Csharp_final_assignment_Face_Recognition_Attendance_System.ViewModel"
        mc:Ignorable="d"
        Title="人脸识别考勤系统" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        WindowStartupLocation="CenterScreen">
    <ContentControl Content="{Binding CurrentViewModel}">
        <ContentControl.Resources>
            <DataTemplate DataType="{x:Type vm:HomeViewModel}">
                <local:Home />
             </DataTemplate>
            <DataTemplate DataType="{x:Type vm:EmployeeLoginViewModel}">
                 <local:EmployeeLogin />
             </DataTemplate>
                
            <DataTemplate DataType="{x:Type vm:LoginViewModel}">
                <local:Login />
            </DataTemplate>
                
            <DataTemplate DataType="{x:Type vm:AdminViewModel}">
                <local:Admin />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PunchCardModelView}">
                <local:PunchCard/>
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>
</Window>
